# Диагностика (Шаг 1) — результат

## Проверки выполнены

| Проверка | Результат |
|----------|-----------|
| Бэкенд `/mini-app/init` | **OK** — GET → 405 (ожидаемо), POST с пустым body → 401 "initData пустой". Сервер доступен. |
| CORS `OPTIONS /mini-app/save-test-result` с Origin Vercel | **OK** — 204, заголовки `Access-Control-Allow-Origin: *`, `Access-Control-Allow-Methods: GET, POST, OPTIONS`. |
| Vercel Production URL | **ПРОБЛЕМА** — `https://path-to-self-miniapp-ozharov164-9998s-projects.vercel.app` → **404**, `x-vercel-error: DEPLOYMENT_NOT_FOUND`. Деплой на production отсутствует или не привязан к домену. |
| API save/history (без валидного токена) | **OK** — POST с фейк-токеном → 401 "Invalid token" (ожидаемо). Логика эндпоинтов работает. |

## Вывод

**Проблемы:**
1. **Vercel:** по Production URL отдаётся 404 (DEPLOYMENT_NOT_FOUND). Нужен успешный деплой и привязка к production (Redeploy или push в main после подключения Git).
2. HTTP бэкенда: бэкенд по `http://217.114.11.97:8080` — для продакшена часто нужен HTTPS; пока оставляем HTTP, фронт уже настроен на этот URL.

**Готов исправлять:** запуск Redeploy через API (нужен VERCEL_TOKEN) или push в main для триггера деплоя на Vercel.
