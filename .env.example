# Backend URL for Mini App (save-test-result, test-history, init).
# Used at build time (Vite embeds it).
VITE_BOT_BACKEND_URL=http://217.114.11.97:8080
# Groq / нейросеть: ключи задаются только на сервере бота в /opt/bot/.env
# (GROQ_API_KEY и GROQ_API_KEYS через запятую). Фронт к Groq не ходит.
